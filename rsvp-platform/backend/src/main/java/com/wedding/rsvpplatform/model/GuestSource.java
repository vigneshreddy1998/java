package com.wedding.rsvpplatform.model;

public enum GuestSource {
    /** Came from your phone contacts import. */
    IMPORTED,
    /** Found the link and RSVP'd without being on your list. */
    SELF_REGISTERED
}
